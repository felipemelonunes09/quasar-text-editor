import config
import tkinter as tk

icons_map = {
    "file-plus": tk.PhotoImage(config.FILE_PLUS_ICON_PATH),
    "file-edit": tk.PhotoImage(config.FILE_EDIT_ICON_PATH),
    "project": tk.PhotoImage(config.PROJECT_ICON_PATH)
}